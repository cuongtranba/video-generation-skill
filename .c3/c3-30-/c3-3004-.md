---
id: c3-3004
c3-seal: fdce4baa0d508a9b96e58920ea803b5b676a29e4290267a0649aee1de863283d
title: PipelineCard — per-project pipeline board card
type: component
parent: c3-30
goal: 'Render one project as a full-width pipeline board: six step nodes (script → material → voiceover → captions → approval gate → render), a step detail panel, and the NATS worker-event log.'
uses:
    - rule-no-any-data
---

## Goal

Render one project as a full-width pipeline board: six step nodes (script → material → voiceover → captions → approval gate → render), a step detail panel, and the NATS worker-event log.

## Parent Fit

| Field | Value |
| --- | --- |
| Container | c3-30 frontend |
| Category | feature |
| Boundary | Browser React component; composes PipelineNode, StepDetail, EventLog; dispatches commands via Zustand store |
| Status | active |

## Purpose

Owns PipelineCard: the card header (id, status tally, idea, cost cap, CostBadge), the node rail with flow edges, and the detail+log footer. Derives per-step state from ProjectState plus the raw event log via the pipeline model component. Non-goal: does not own step visualization internals or the pure derivation — those are PipelineNode/StepDetail/EventLog and the pipeline model component.

## Governance

| Reference | Type | Governs | Precedence | Notes |
| --- | --- | --- | --- | --- |
| rule-no-any-data | rule | Props typed with ProjectState; action handlers typed | high | N.A - no additional notes |

## Contract

| Surface | Direction | Contract | Boundary | Evidence |
| --- | --- | --- | --- | --- |
| PipelineCard | OUT | Renders one project as a pipeline board (node rail + step detail + event log) | in-process | frontend/src/components/PipelineCard.tsx |

| CostBadge | OUT | Renders spent USD cost formatted as $N.NN | in-process | frontend/src/components/CostBadge.tsx |

## Derived Materials

| Material | Must derive from | Allowed variance | Evidence |
| --- | --- | --- | --- |
| Node selection + detail routing | Contract | N.A - active step derived when no node is selected | frontend/src/components/PipelineCard.tsx |
