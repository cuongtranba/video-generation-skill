fact: rule-cost-wall
claim: "cost cap is enforced before TTS job dispatch in generateVoiceovers"
code:
  - api/src/cost.ts
  - api/src/commands.ts
pipeline:
  - gather: { file: api/src/cost.ts }
  - eval:   { contains_all: ["DEFAULT_COST_CAP_USD", "admit", "CostCapExceededError"] }
