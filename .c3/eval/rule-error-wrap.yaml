fact: rule-error-wrap
claim: "errors are wrapped with op context via fmt.Errorf(...: %w)"
code:
  - worker/internal/jobhandler/material.go
pipeline:
  - gather: { file: worker/internal/jobhandler/material.go }
  - eval:   { contains: "%w" }
