fact: rule-no-any-data
claim: "bus messages are concrete typed structs, not map[string]any"
code:
  - internal/worker/types.go
pipeline:
  - gather: { file: internal/worker/types.go }
  - eval:   { contains: "type TTSJob struct" }
