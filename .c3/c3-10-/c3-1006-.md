---
id: c3-1006
c3-seal: ba85e0b2cf05e3d429b1d87bf7981c5d250f23163247d0c9047fbcf3e5fd677f
title: cost — budget estimator and enforced cost wall
type: component
parent: c3-10
goal: Project TTS cost and enforce the per-video cost cap before dispatching voiceover jobs; provide the cost ledger read path from Postgres.
uses:
    - rule-cost-wall
---

## Goal

Project TTS cost and enforce the per-video cost cap before dispatching voiceover jobs; provide the cost ledger read path from Postgres.

## Parent Fit

| Field | Value |
| --- | --- |
| Container | c3-10 api |
| Category | foundation |
| Boundary | In-process module; called by commands.ts generateVoiceovers; reads cost_ledger table |
| Status | active |

## Purpose

Owns projectedTtsUsd (per-character TTS_USD_PER_CHAR = $0.00001), admit (dry-run cost gate), CostCapExceededError, costCapFromEnv (COST_CAP_USD env, default $0.15), and readLedger (Postgres cost_ledger query). Non-goal: the cap constant is configuration, not a compile-time value; admin can raise it via env.

## Governance

| Reference | Type | Governs | Precedence | Notes |
| --- | --- | --- | --- | --- |
| rule-cost-wall | rule | admit() must be called and its result checked before any TTS job dispatch; CostCapExceededError must not be caught silently | high | The cost wall is the inviolable guard |

## Contract

| Surface | Direction | Contract | Boundary | Evidence |
| --- | --- | --- | --- | --- |
| admit | OUT | Returns {admitted, projectedUsd, capUsd}; commands.ts must throw CostCapExceededError when !admitted | in-process | api/src/cost.ts |
| TTS_USD_PER_CHAR | OUT | $0.00001 per character; mirrors worker jobhandler ttsUsdPerChar | config | api/src/cost.ts |
| DEFAULT_COST_CAP_USD | OUT | $0.15 default; overridden by COST_CAP_USD env | config | api/src/cost.ts |

## Derived Materials

| Material | Must derive from | Allowed variance | Evidence |
| --- | --- | --- | --- |
| TTS_USD_PER_CHAR | Contract | N.A - both must match | api/src/cost.ts |
