---
id: c3-110
c3-seal: 4a174155e9b5a7e89b40d9613df6f77b1e5bc9f928758676e3d45ab2259640ea
title: flow — pipeline status machine & orchestrator
type: component
parent: c3-1
goal: Orchestrate the end-to-end project lifecycle as a resumable status machine, enforcing cost gates and coordinating every pipeline step.
uses:
    - ref-idempotent-worker
    - ref-manifest-state
    - rule-cost-wall
    - rule-error-wrap
---

## Goal

Orchestrate the end-to-end project lifecycle as a resumable status machine, enforcing cost gates and coordinating every pipeline step.

## Parent Fit

| Field | Value |
| --- | --- |
| Parent | c3-1 vidgen CLI process |
| Layer | Feature — the business orchestrator over all foundation + provider components |
| Depends on | c3-101 domain, c3-104 cost, c3-201 bus, c3-210 worker, script/material/audio/visual/publish |
| Consumed by | c3-106 cli (each subcommand calls a flow step) |

## Purpose

Owns `Flow` and its step methods (create/material/tune/confirm/generate/publish), advancing `Project.Status` through `draft→material→tuned→confirmed→rendered→published`, saving the manifest after each step, projecting cost at confirm and enforcing actual cost mid-generate. During `generate` it starts the embedded bus and drives per-scene fan-out. Non-goals: no vendor API details — those live in provider components.

## Governance

| Reference | Type | Governs | Precedence | Notes |
| --- | --- | --- | --- | --- |
| ref-manifest-state | ref | Save after every step; status gates ordering | Authoritative | flow is the status machine |
| rule-cost-wall | rule | Confirm calls CheckProjected, generate calls CheckActual | Authoritative | never bypass a check |
| ref-idempotent-worker | ref | Generate re-runs at $0 by relying on worker skip | Must | resumable generate |
| rule-error-wrap | rule | Every step error wrapped with project + op context | Must | includes project id |

## Contract

| Surface | Direction | Contract | Boundary | Evidence |
| --- | --- | --- | --- | --- |
| Flow.Confirm(ctx, p) | IN/OUT | Projects cost, CheckProjected, freezes projection, status→confirmed | Requires status tuned | internal/flow/flow.go |
| Flow.Generate(...) | IN/OUT | Runs TTS→caption→render via bus, enforcing actual cost | Requires status confirmed | internal/flow/generate.go |
| status transitions | OUT | Only forward moves through the defined sequence | draft→…→published | internal/flow/flow.go |

## Derived Materials

| Material | Must derive from | Allowed variance | Evidence |
| --- | --- | --- | --- |
| flow step tests | Contract | mocked providers | internal/flow/flow_test.go |
