---
id: c3-115
c3-seal: 9aae2ddfcec642b295e7a10bfcfb48c42d7393e6038ece37226b6a99ce30eed2
title: publish — upload finished video to platform
type: component
parent: c3-1
goal: Upload the rendered 9:16 MP4 to a destination platform and record the published state.
uses:
    - ref-provider-seam
    - rule-di-constructor
    - rule-error-wrap
---

## Goal

Upload the rendered 9:16 MP4 to a destination platform and record the published state.

## Parent Fit

| Field | Value |
| --- | --- |
| Parent | c3-1 vidgen CLI process |
| Layer | Feature — terminal pipeline step, status→published |
| Depends on | c3-102 config (publish selection + token), TikTok API |
| Consumed by | c3-110 flow (publish step), c3-106 cli (publish command) |

## Purpose

Owns `Publisher` and its `TikTokPublisher` implementation (upload via the TikTok API using `TIKTOK_ACCESS_TOKEN`), selected via `NewFromConfig` with a `none` default. Non-goals: no rendering, no cost accounting — upload only.

## Governance

| Reference | Type | Governs | Precedence | Notes |
| --- | --- | --- | --- | --- |
| ref-provider-seam | ref | Publisher built via NewFromConfig; token from .env | Authoritative | tiktok / none selectable |
| rule-di-constructor | rule | var _ Publisher = (*TikTokPublisher)(nil); injected token | Must | compile-time seam check |
| rule-error-wrap | rule | Upload/API errors wrapped with op context | Must | wraps HTTP failures |

## Contract

| Surface | Direction | Contract | Boundary | Evidence |
| --- | --- | --- | --- | --- |
| Publisher.Publish(ctx, videoPath) | IN/OUT | Uploads the MP4, returns a platform reference | Requires TIKTOK_ACCESS_TOKEN | internal/publish/tiktok.go |
| NewFromConfig(sel) | IN | Builds the selected publisher; none is a no-op default | tiktok, none | internal/publish/publish.go |

## Derived Materials

| Material | Must derive from | Allowed variance | Evidence |
| --- | --- | --- | --- |
| publish tests | Contract | httptest-stubbed TikTok API | internal/publish/tiktok_test.go |
