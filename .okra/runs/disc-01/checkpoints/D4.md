# D4 checkpoint — Go worker ↔ event store
- Go `nats.go/jetstream` publishes result events; `WithMsgID` + stream dupe-window gives event-level idempotency (replaces output-file check).
- Ordered consumer with FilterSubjects reads per-subject history — usable for replay-safe consume.
- Verified API signatures against context7 (`/nats-io/nats.go`, `jetstream/README.md` + `jetstream/MIGRATION.md`): the skeleton's signatures matched as given — `js.Publish(ctx, subject, data, jetstream.WithMsgID(id)) (*jetstream.PubAck, error)`, `js.OrderedConsumer(ctx, streamName, jetstream.OrderedConsumerConfig{FilterSubjects: []string{...}}) (jetstream.Consumer, error)`, `consumer.Fetch(batchSize) (jetstream.MessageBatch, error)` with `batch.Messages()` as a ranged channel. No deviations required; added a `batch.Error()` check after draining `Messages()` per the docs' recommended pattern (not fatal to the spike, just defensive).
- Runtime note: `Fetch(10)` against a subject with only 1 stored message blocks for the fetch's default max-wait (~30s) before returning, since the requested batch size exceeds available messages — the test still passes but is slow. A real worker/consumer should use `jetstream.FetchMaxWait(...)` tuned low, or `PullMaxMessages` sized to expected volume, to avoid this latency in production code.
- Open: job-consume ack policy + retry (defer to impl); worker reads job payloads carrying all needed data (no DB coupling).
- Confidence: HIGH — dedup test green.
